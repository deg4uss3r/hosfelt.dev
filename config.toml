# The URL the site will be built for
base_url = "Ricky.Hosfelt.dev/test/"
compile_sass = false
build_search_index = false
theme = "boring"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "dracula"
# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

[extra]
homepage_title = "Hello, I am a boring guy!"
homepage_subtitle = "I love to sit around all day, doing nothing."
homepage_img_url = "/homepage-img.jpg"

# Refer https://icons8.com/line-awesome for list of icons
# Both `url` and `icon` should be set for each entry
[[extra.social]]
url = "https://github.com/deg4uss3r"
icon = "lab la-github"

[[extra.social]]
url = "https://fosstodon.org/@degausser"
icon = "lab la-mastodon"
